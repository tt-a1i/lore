{
  "name": "lore",
  "version": "0.0.1",
  "description": "The intent layer for Git — link AI agent conversations to the commits they produced",
  "type": "module",
  "license": "Apache-2.0",
  "bin": {
    "lore": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "test:kuzu": "tsx scripts/test-kuzu-store.mts",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "commander": "^13.0.0",
    "kuzu": "^0.11.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
